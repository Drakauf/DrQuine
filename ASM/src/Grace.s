# make 3 defines
 ->> char to print
 ->> print function
 ->> is a macro a define ?
# make a macro for main
 ->> need fopen, fprintf, fclose
 ->> need to save return value of fopen
 ->> if return value of fopen is 0, exit the programm
 ->> else write in file
 ->> close file
# call main macro
